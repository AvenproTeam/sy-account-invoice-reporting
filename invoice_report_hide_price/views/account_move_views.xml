<?xml version="1.0" encoding="utf-8" ?>
<!-- Copyright 2022 Manuel Regidor <manuel.regidor@sygel.es>
     License AGPL-3.0 or later (https://www.gnu.org/licenses/agpl). -->
<odoo>
    <record id="invoice_report_hide_price_view_move_form" model="ir.ui.view">
        <field name="name">invoice.report.hide.price.view.move.form</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form" />
        <field name="arch" type="xml">
            <!-- Buttons in header -->
            <xpath expr="//header/button[@name='action_post']" position="after">
                <button
                    name="action_activate_show_price_info_report"
                    type="object"
                    string="Show Lines Price Info in Report"
                    invisible="move_type == 'entry'"
                />
                <button
                    name="action_deactivate_show_price_info_report"
                    type="object"
                    string="Hide Lines Price Info in Report"
                    invisible="move_type == 'entry'"
                />
            </xpath>
            <!-- Add field to line_ids list (hidden) so it's saved correctly -->
            <xpath expr="//field[@name='line_ids']/list/field[@name='display_type']" position="after">
                <field name="show_price_info_report" column_invisible="1" />
            </xpath>
            <!-- Add visible field to invoice_line_ids, anchored before display_type -->
            <xpath expr="//field[@name='invoice_line_ids']/list/field[@name='display_type']" position="before">
                <field name="show_price_info_report" />
            </xpath>
        </field>
    </record>
</odoo>
